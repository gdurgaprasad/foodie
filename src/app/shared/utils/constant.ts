export enum SNACKBAR_MESSAGES {
  ADD_TO_CART_SUCCESS = 'added to cart successfully.',
  REMOVE_PRODUCT_FROM_CART = 'removed from cart successfully.',
  ORDER_SUCCESS = 'Woah! Your delicious food order is placed.',
}

export enum RESPONSES {
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED ',
}

export enum PAGES {
  CART = '/cart',
  HOME = '/home',
  HISTORY = '/order-history',
}

export enum MESSAGES {
  NETWORK_NOT_AVAILABLE = 'No Internet connection.',
}
